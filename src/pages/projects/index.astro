---
import ContentLayout from "@/layouts/ContentLayout.astro";
import H1 from "@/components/H1.astro";
import CategoryFilter from "@/components/CategoryFilter.astro";
import ProjectCard from "@/components/ProjectCard.astro";
import { getPublishedProjects, getCategories } from "@/lib/content";

const projects = await getPublishedProjects();
const categories = getCategories(projects);
---

<ContentLayout
  title="Projects - Hugo Charrade, Full-Stack Developer"
  description="Explore projects by Hugo Charrade, a Full-Stack Developer specializing in building fast and scalable web applications."
>
  <H1>Projects</H1>

  <div class="space-y-4 sm:space-y-12 mt-4">
    <CategoryFilter categories={categories} basePath="/projects" />

    <ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {
        projects.map((project) => (
          <li>
            <ProjectCard project={project} />
          </li>
        ))
      }
    </ul>
  </div>
</ContentLayout>
